# Private Implementation

The implementation must preserve non-regression of published time, exact rational source conversion and separation between the best accepted correlation and the currently publishable disciplined timeline. Synchronization updates use one-writer/many-reader publication rather than exposing partially updated discipline state. ClockCorrelation is immutable once captured.
