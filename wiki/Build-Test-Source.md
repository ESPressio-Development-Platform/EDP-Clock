# Build, Test and Source Map

C++20 is required. `src/` owns public clock contracts and implementation headers; `docs/` covers monotonic clock, synchronized clock, correlation and stopwatch semantics; `tests/` validates conversion, non-regression, state/uncertainty behaviour and stopwatch semantics; `demos/` exercises supported integration paths.
